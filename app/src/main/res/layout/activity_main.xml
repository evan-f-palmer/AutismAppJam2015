<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:id="@+id/layout"
    android:orientation="vertical"
    android:background="@drawable/background">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Draw"
        android:id="@+id/drawButton"
        android:onClick="drawButtonClicked"
        android:layout_marginBottom="43dp"
        android:layout_above="@+id/moodButton"
        android:layout_alignLeft="@+id/viewMoodsButton"
        android:layout_alignStart="@+id/viewMoodsButton" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mood"
        android:id="@+id/moodButton"
        android:onClick="moodButtonClicked"
        android:layout_centerVertical="true"
        android:layout_alignLeft="@+id/drawButton"
        android:layout_alignStart="@+id/drawButton" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="view"
        android:id="@+id/viewMoodsButton"
        android:onClick="viewMoodsButtonClicked"
        android:layout_marginTop="59dp"
        android:layout_below="@+id/moodButton"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
